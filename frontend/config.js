// Frontend Configuration
// Set API_URL to your deployed backend URL before running.

const CONFIG = {
    FACEBOOK_APP_ID: 'your_facebook_app_id',   // developers.facebook.com > App Settings > Basic
    API_URL: 'http://localhost:8001/api'         // change to production backend URL when deploying
};
